export type IEventCategory = {
  id: string;
  name: string;
  createdAt: string;
  eventCount: number;
};

export type ITrendingEventCategory = {
  id: string;
  name: string;
  eventCount: number;
};
